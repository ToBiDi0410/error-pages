- var code = 409;
- var title = "Conflict!";
- var description = "The request could not be completed due to a conflict with the current state";
- var enduserReasons = ['The action conflicts with existing data'];
- var enduserAdvices = ['Resolve any duplicate or conflicting values'];
- var professionalReasons = ['The request would violate a unique constraint', 'A conflicting operation is in progress'];
- var professionalAdvices = ['Check for existing resources before creation', 'Handle concurrency and uniqueness conflicts'];
- var img = "data-url:http-4XX.jpeg";
include ../http-error.pug